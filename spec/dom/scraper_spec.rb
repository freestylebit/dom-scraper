require "spec_helper"

describe Dom::Scraper do
  it "has a version number" do
    expect(Dom::Scraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
